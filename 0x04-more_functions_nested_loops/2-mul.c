#include "main.h"
/**
 * mul - Check description
 * Description: It a function that multiplies two integers.
 * @a : input number
 * @b : input number
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
