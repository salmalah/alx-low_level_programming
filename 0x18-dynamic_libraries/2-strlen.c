#include "main.h"
#include <string.h>
/**
 * _strlen -  a function that returns the length of a string
 * @s: An input string
 * Return: Nothing
 */
int _strlen(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
