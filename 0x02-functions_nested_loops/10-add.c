#include "main.h"
/**
 * add - Check description
 * @x: input number
 * @y: input number
 * Descripton: It a function that adds two integers and returns the result.
 * Return: 0 if seccussful else 1
 */
int add(int x, int y)
{
	int r;

	r = x + y;
	return (r);
}
